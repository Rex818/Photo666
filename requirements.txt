# Photo666 v0.3.0 Dependencies
# ========================================
# Core Dependencies
# ========================================

# Core GUI Framework
PyQt6>=6.5.0

# Image Processing
Pillow>=10.2.0

# Configuration Management
PyYAML>=6.0.0

# Database Utilities
sqlite-utils>=3.35.0

# Translation Support
googletrans-py>=4.0.0

# Async Support
asyncio-mqtt>=0.11.0
aiofiles>=22.0.0

# ========================================
# AI Model Dependencies
# ========================================

# PyTorch (CUDA 12.8 compatible, supports GPU inference)
# Note: These versions are optimized for CUDA 12.8 to ensure GPU acceleration
# Versions adjusted based on actual virtual environment configuration
torch>=2.7.0+cu128
torchvision>=0.22.0+cu128
torchaudio>=2.7.0+cu128

# Alternative installation methods if the above versions fail:
# Method 1: Install from PyTorch official (recommended)
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Method 2: Install using conda
# conda install pytorch torchvision torchaudio pytorch-cuda=12.8 -c pytorch -c nvidia

# Transformers (AI model framework)
# Version adjusted based on actual virtual environment configuration
transformers>=4.55.0

# Hugging Face Hub
huggingface_hub>=0.16.0

# Accelerate (model acceleration)
# Version adjusted based on actual virtual environment configuration
accelerate>=1.9.0

# Safetensors (model file format)
safetensors>=0.3.0

# Bitsandbytes (quantization support)
bitsandbytes>=0.41.0

# ========================================
# Computer Vision Dependencies
# ========================================

# NumPy (numerical computing)
numpy>=1.24.0

# Einops (tensor operations)
einops>=0.8.0

# Timm (image models)
timm>=1.0.0

# ========================================
# Network and Utility Dependencies
# ========================================

# HTTP requests
requests>=2.28.0

# Geographic calculations
geopy>=2.3.0

# Progress bar
tqdm>=4.60.0

# ========================================
# Plugin Dependencies Integration
# ========================================
# The following dependencies cover all plugin requirements:
# - florence2_reverse_plugin: transformers, torch, torchvision, accelerate, safetensors, pillow, numpy, einops, timm
# - joycaption_reverse_plugin: torch, transformers, huggingface_hub, pillow, requests, bitsandbytes, accelerate, safetensors
# - janus_reverse_plugin: torch, transformers, pillow, numpy, requests, tqdm (Janus code integrated)
# - janus_text2image_plugin: pillow (Janus code integrated)
# - gps_location_plugin: requests, geopy

# ========================================
# Special Dependencies
# ========================================

# Janus AI model code is integrated in janus_text2image_plugin
# No need to download from GitHub repeatedly

# ========================================
# Important Notes
# ========================================
# 1. First installation may take a long time due to AI model downloads
# 2. Recommended to install in virtual environment
# 3. If network issues occur, use domestic mirror sources
# 4. CUDA version requirement: CUDA 12.8 or compatible version
# 5. GPU support: Ensure PyTorch CUDA version is installed for GPU acceleration
# 6. If pip installation of CUDA version fails, use PyTorch official command:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# 7. Janus-related functions are integrated in plugins, no additional installation needed
# 8. Verify GPU support: python -c "import torch; print('CUDA available:', torch.cuda.is_available())"
